# linear-regression-from-scratch
This project demonstrates how to implement Linear Regression entirely from scratch using NumPy, without relying on libraries like scikit-learn. It uses the Normal Equation to compute the optimal weight vector that minimizes the Sum of Squared Errors (SSE) between actual and predicted values.
